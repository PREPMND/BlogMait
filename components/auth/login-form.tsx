import { string } from "@preplabs/validator";
import {z} from "zod";

const LoginSchema=({
    email:string;
    password:string;
})
export default function LoginForm(){
    return (
        <div>
            
        </div>
    )
}